---
description: "[standalone] Core rules — when workspace=cattr/ see monorepo root"
alwaysApply: true
---

# Standalone workspace note

When workspace is `cattr/` (monorepo), rules load from `cattr/.cursor/rules/`.
This file applies only when opening `desktop-application/` as the workspace root.

Shared conventions: [AGENTS.md](../../../AGENTS.md)

- Minimal diffs; match existing patterns
- No commits unless explicitly requested
- Never commit `.env`, secrets, `.project`, `.settings/`
- License: SSPL-1.0
